---
const { icon: Icon, name, url } = Astro.props;
---

<article
  class="flex flex-col py-8 bg-special border-1 border-white/10 rounded-xl
  hover:-translate-y-2 hover:shadow-2xl hover:shadow-light-blue/20 transition-all cursor-pointer"
>
  <a href={url} class="flex flex-col items-center gap-4">
    <Icon class="size-14" />
    <h4 class="text-2xl font-semibold">{name}</h4>
  </a>
</article>
