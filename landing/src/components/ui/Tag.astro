---
const { icon: Icon, label, className } = Astro.props;
---

<div
  class={`flex items-center gap-2 bg-dark-blue border-1 border-gray-800 px-6 py-2 rounded-full text-sm hover:-translate-y-1 hover:border-white/40 transition-all ${className}`}
>
  {Icon && <Icon class="size-5 text-light-blue" />}
  <span class="text-white">{label}</span>
</div>
